<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fall.robok.mapper.UserMapper">
    <!-- public User getAllUser(); -->
    <select id="getAllUser" resultType="com.fall.robok.model.User">
        select *
        from t_user
    </select>
    <!-- Integer userSignUp(String code,String nickName) -->
    <insert id="userSignUp">

    </insert>
    <!-- Integer userSignIn(String code,String nickName) -->
    <select id="userSignIn" resultType="int">

    </select>
</mapper>